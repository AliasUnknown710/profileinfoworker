{
  "name": "profile-info-worker",
  "version": "1.0.0",
  "description": "A secure Cloudflare Worker for retrieving user profile information with authentication and backend integration",
  "main": "ProfileInfoWorker.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "setup": "cp wrangler.toml.example wrangler.toml && echo 'Please edit wrangler.toml and set your backend URL with: wrangler secret put BACKEND_URL'",
    "test": "echo 'Open test.html in browser to test worker functionality'",
    "validate": "echo 'Run security validation tests'"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "profile",
    "user-info",
    "authentication",
    "security",
    "backend-integration"
  ],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/username/user-management-workers.git",
    "directory": "ProfileInfoWorker"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "wrangler": "^3.0.0"
  }
}
